import styled from 'styled-components';

import { GRADIENT_TWO, CARD_TEXT } from '../../config/styleConstants.js';

export const StyledSelect = styled.select`
  width: 200px;
  height: 40px;
  font-size: 20px;
  background-color: ${GRADIENT_TWO};
  color: ${CARD_TEXT};
`